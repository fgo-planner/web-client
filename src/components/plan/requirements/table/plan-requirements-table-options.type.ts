export type PlanRequirementsTableOptions = {

    layout?: 'condensed' | 'normal';

    displayItems: {
        unused?: boolean;
        statues?: boolean;
        gems?: boolean;
        lores?: boolean;
        grails?: boolean;
        embers?: boolean;
        fous?: boolean;
    }

};
