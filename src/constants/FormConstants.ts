/**
 * Value assigned to a blank option in a MUI Select field. This is a workaround
 * to fix a styling issue where the input field's label overlaps with the
 * outline if the actual value is an empty string.
 */
export const BlankOptionValue = '_blank';
