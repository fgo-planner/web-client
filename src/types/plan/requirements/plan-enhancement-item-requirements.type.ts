/**
 * Breakdown of the quantities required for each enhancement category for a
 * single item.
 */
export type PlanEnhancementItemRequirements = {
    ascensions: number;
    skills: number;
    appendSkills: number;
    costumes: number;
    total: number;
};
